# Binario compilado no Mac pelo mesa-dj.command (especifico da arquitetura,
# nao deve ser versionado — cada maquina compila o seu).
mesa-dj-mac

# Diretorio de build do CMake
build/

# Artefatos comuns de objeto/depuração
*.o
*.obj
*.dSYM/